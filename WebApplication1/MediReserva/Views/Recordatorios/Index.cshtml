@model IEnumerable<WebApplication1.Models.Recordatorio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Mensaje)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaDeEnvio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCitaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Mensaje)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaDeEnvio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCitaNavigation.IdCita)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.IdRecordatorio">Edit</a> |
                <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.IdRecordatorio">Details</a> |
                <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.IdRecordatorio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
